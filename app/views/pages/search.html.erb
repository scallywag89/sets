<%= render 'shared/searchbar' %>
<% if @albums %>
  <% @albums.each do |album| %>
    <%= render 'shared/album_card', album: album %>
    <% raise %>
  <% end %>
<% end %>


<%# album.new(
  title: result.title,
  artist:
) %>
<%# @search.results.each do |result| %>
    <%# if result.type == "master" || result.type == "release" %>
    <%#= album_card(Album.new(title: result.title, )) %>
